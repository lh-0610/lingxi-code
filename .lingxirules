# 灵犀项目规则

## 单一事实源

- 开始任务前先阅读项目根 `CLAUDE.md`，架构、线程、状态和开发约定以它为准。
- `AGENTS.md` 只负责指向 `CLAUDE.md`，不要在多份文档中重复维护同一约定。
- 修改子目录文件前，先调用 `get_project_instructions(path)` 检查该路径是否有更具体的规则。

## 工作方式

- 先读现有实现和相关测试，再决定怎么改；不要按常见项目结构猜文件位置。
- 需要 3 步以上或跨文件修改时使用 `update_plan`。
- 更新计划时默认只修改 `[ ]` / `[~]` / `[x]` 状态，不要随意改写或删除未完成步骤。
- 如确需合并、拆分或取消普通步骤，必须在计划中写明 `调整说明：...`。
- 测试、全量测试、`git diff` 等验收步骤不得静默删除。
- 不做与任务无关的重构，不覆盖或撤销用户已有改动。

## 代码约定

- Python 3.14；不要使用已废弃或不兼容的新旧 LangChain API。
- 全局共享状态放 `src/state.py`；会话级状态放 `src/session.py`，通过现有 state 代理访问。
- 写会话级状态时必须考虑 worker 线程绑定和多会话隔离，不能串到 active session。
- UI worker 线程不得直接操作 Qt widget、QTimer 或 repaint；必须通过 Signal 投递到主线程。
- 文件路径统一基于当前会话项目根解析，使用 `realpath/commonpath` 防止 `..`、绝对路径和符号链接越界。
- 新增工具必须检查并按需注册到：
  - `ALL_TOOLS`
  - `TOOL_DISPLAY_NAMES`
  - `PLAN_MODE_READONLY_TOOLS`
  - `PARALLEL_SAFE_TOOLS`
  - `NO_ARG_OK_TOOLS`
- 只读工具不得修改全局状态；写工具必须保留现有确认、checkpoint 和验证流程。
- 改已有代码优先用精确编辑或原子 patch，不要读取部分文件后用全量覆盖重写。
- 保持现有中文提示和日志风格，注释只解释非显然的约束。

## 调研与测试

- 修改代码前，优先调用 `related_files(path)` 判断影响范围。
- 跑测试前，调用 `find_tests(path, symbol)` 查找相关测试，不要默认猜 `tests/`；本仓库测试主要在 `scripts/`。
- 先跑相关测试，再根据改动范围决定是否跑全量。
- Python 修改后至少通过语法/静态检查。
- 逻辑修改完成前必须：
  1. 跑相关测试；
  2. 必要时跑全量测试；
  3. 调用 `git_diff` 查看最终改动。
- 测试失败要继续修；确实无法运行时，最终回复必须明确说明原因和风险。
- 不得把测试未运行、失败或结果未知描述为“全部通过”。

## Review 重点

- Review 时优先找行为 bug、状态串线、线程问题、路径越界、数据丢失和缺失测试。
- 不要只看测试是否通过；要实际调用新增工具或走一遍关键流程，确认功能不是“代码存在但不可达”。
- 对模型输出解析、计划、历史压缩等功能，必须测试污染输入和边界输入。

## Git 安全

- 暂存 / 提交只能通过 `git_stage` / `git_unstage` / `git_commit` 工具，且**每次执行前都会弹确认卡、必须经用户批准才执行**；不要主动提交，也不要用 `run_command` 跑 `git add/commit` 绕过确认卡。
- 没有 push 工具：`git push`、`reset`、`checkout --` 一律不执行（无工具，也不得用 `run_command` 代跑），除非用户明确要求。
- 不使用 `git add .` / 全量暂存；按文件路径精确暂存。
- 不覆盖用户任务开始前已有的未提交修改。
- 最终汇报列出实际修改文件、相关测试、全量测试和剩余限制。
